server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #服务注册中心地址
      config:
        server-addr: localhost:8848 #配置中心地址
        file-extension: yml #指定yaml格式的配置
        group: TEST_GROUP #分组
        #group: PROD_GROUP #分组
        namespace: c582ac0d-f05e-4879-8488-9af0300ca084


# nacos-config-client-dev.yml